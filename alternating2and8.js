// Create a function that takes a number n (integer greater than zero) as an argument,
// and returns 2 if n is odd and 8 if n is even.

// You can only use the following arithmetic operators:
// addition of numbers +, subtraction of numbers -, multiplication of number *,
// division of number /, and exponentiation **.

// You are not allowed to use any other methods in this challenge
// (i.e. no if statements, comparison operators, etc).

// Solution
// In progress

// Tests
console.log(f(1), 2);
console.log(f(2), 8);
console.log(f(3), 2);
console.log(f(4), 8);
console.log(f(100), 8);
console.log(f(101), 2);
console.log(f(2567), 2);
console.log(f(2367134), 8);
